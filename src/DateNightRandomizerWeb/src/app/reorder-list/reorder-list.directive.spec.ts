import { ReorderListDirective } from './reorder-list.directive';

describe('ReorderListDirective', () => {
  it('should create an instance', () => {
    /*
    const directive = new ReorderListDirective();
    expect(directive).toBeTruthy();
    */
  });
});
